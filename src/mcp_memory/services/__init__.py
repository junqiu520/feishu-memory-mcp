"""Service layer — business-logic orchestration that ties adapters and storage together.

Stage 5 implements the local orchestration skeleton. Feishu SDK calls remain
unwired (Stage 8 integration). The interfaces here are what Stage 6 (MCP tools)
and Stage 7 (CLI) consume.
"""
